# Omarchy Aliases
[ -f "$OMARCHY_PATH/default/bash/aliases" ] && source "$OMARCHY_PATH/default/bash/aliases"

# User Custom Aliases
[ -f ~/.bash_aliases ] && source ~/.bash_aliases

# Add more aliases here if you want
