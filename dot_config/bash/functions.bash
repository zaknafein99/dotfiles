# Omarchy Shell & Functions
[ -f "$OMARCHY_PATH/default/bash/shell" ] && source "$OMARCHY_PATH/default/bash/shell"
[ -f "$OMARCHY_PATH/default/bash/functions" ] && source "$OMARCHY_PATH/default/bash/functions"

# Custom Functions
# (Add your own bash functions here)
